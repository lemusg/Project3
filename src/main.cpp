//Gabriel Lemus

#include "WalkMap.h"
#include "BFS.h"
#include <iostream>

using namespace std;

int main() {
    //
    int mapSize, numConnections;
    cin >> mapSize;
    cin >> numConnections;
    WalkMap map(mapSize, numConnections);
}